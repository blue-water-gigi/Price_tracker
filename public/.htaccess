RewriteEngine On

# Если файл или папка реально существуют — отдать как есть
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Всё остальное на index.php
RewriteRule ^ index.php [QSA,L]
